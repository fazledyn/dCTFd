version: '2'

services:
  ctfd:
    build: .
    user: root
    restart: always
    ports:
      - "8000:8000"
    environment:
      - WORKERS=1
      - LOG_FOLDER=/var/log/CTFd
      - ACCESS_LOG=-
      - ERROR_LOG=-
      - REVERSE_PROXY=true
      - SECRET_KEY=c4n7_f1nD_0u7_7h15_5h1t_0x1337
      - DATABASE_URL=mysql+pymysql://doadmin:AVNS_3FAey36iLsJn1WiG1po@bcf23-mysql-do-user-14379091-0.b.db.ondigitalocean.com:25060/defaultdb
      - REDIS_URL=rediss://default:AVNS_ANGYKpKMTOUBXNoP4gu@bcf2023-redis-do-user-14379091-0.b.db.ondigitalocean.com:25061
      - UPLOAD_PROVIDER=s3
      - UPLOAD_FOLDER=ctf_contents
      - AWS_ACCESS_KEY_ID=DO00UKLYWFFQ9DW7XZBJ
      - AWS_SECRET_ACCESS_KEY=r8kPYjNkntJnxGD+QQaxGf7gQBWadZ7I219dBrlVx4Q
      - AWS_S3_BUCKET=bcf23-final
      - AWS_S3_ENDPOINT_URL=https://bcf23-final.sgp1.digitaloceanspaces.com
      - AWS_S3_REGION=sgp1
    volumes:
      - .data/CTFd/logs:/var/log/CTFd
      - .data/CTFd/uploads:/var/uploads
      - .:/opt/CTFd:ro
    networks:
        default:
        internal:

  nginx:
    image: nginx:stable
    restart: always
    volumes:
      - ./conf/nginx/http.conf:/etc/nginx/nginx.conf
    ports:
      - 80:80
    depends_on:
      - ctfd

  # db:
  #   image: mariadb:10.4.12
  #   restart: always
  #   environment:
  #     - MYSQL_ROOT_PASSWORD=ctfd
  #     - MYSQL_USER=ctfd
  #     - MYSQL_PASSWORD=ctfd
  #     - MYSQL_DATABASE=ctfd
  #   volumes:
  #     - .data/mysql:/var/lib/mysql
  #   networks:
  #       internal:
  #   # This command is required to set important mariadb defaults
  #   command: [mysqld, --character-set-server=utf8mb4, --collation-server=utf8mb4_unicode_ci, --wait_timeout=28800, --log-warnings=0]

  # cache:
  #   image: redis:4
  #   restart: always
  #   volumes:
  #   - .data/redis:/data
  #   networks:
  #       internal:

networks:
    default:
    internal:
        internal: true
